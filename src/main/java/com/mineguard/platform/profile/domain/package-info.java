/**
 * Profile bounded context — domain layer (reserved).
 *
 * <p>This context is scaffolded but intentionally empty: the current MineGuard
 * frontends do not consume user-profile endpoints yet. The four-layer structure
 * is kept so the context can be populated in a future iteration.</p>
 */
package com.mineguard.platform.profile.domain;
